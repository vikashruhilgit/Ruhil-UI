import { Meta, Canvas, Story, ArgsTable, Source } from '@storybook/addon-docs';
import '../../src/firstech-ui';

<Meta title="Firstech UI/Component/Slide/Doc" />

<style>{`

`}</style>

# Slide `<ft-slide>`

Is a gereric component for slide show case.

## Example Usage

Note: For `boolean` attributes, `true` means simply setting the attribute (e.g. `<ft-yourcomponent prop>`) and `false` means not setting the attribute (e.g. `<ft-yourcomponent>` without `prop` attribute).

### Standard

```html
<ft-slide open>
  <!-- Content start -->
  <ft-button label="label" raised></ft-button>
  <!-- ... -->
  <!-- Content end -->
</ft-slide>
```

<!-- The usage of this "Component" is intentional to enable react-syntax-highlighter's own highlighter

export const Component = () => {
  return <SyntaxHighlighter />;
};
-->

<!-- [Go to specific documentation page](?path=/docs/firstech-ui-component-icon--icon)

<Source id="firstech-ui-component-navigation--default"></Source> -->
